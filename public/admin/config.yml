backend:
  name: github
  repo: ideas4lego/ideas4lego.github.io
  branch: main
  base_url: https://ideas4lego-cms-oauth.ideas4lego.workers.dev
  auth_endpoint: auth

# Транслитерация кириллицы в имена файлов (slug), чтобы заголовки на
# русском не создавали файлы с русскими именами.
slug:
  encoding: "ascii"
  clean_accents: true

# Показывать превью сайта прямо в CMS
site_url: https://ideas4lego.github.io
display_url: https://ideas4lego.github.io

media_folder: "public/uploads"
public_folder: "/uploads"

# Локализация интерфейса CMS на русский
locale: "ru"

collections:
  - name: "posts"
    label: "Посты"
    label_singular: "Пост"
    folder: "src/content/posts"
    create: true
    delete: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}} — {{date}}"
    sortable_fields: ["date", "title"]
    fields:
      - { label: "Заголовок", name: "title", widget: "string" }
      - { label: "Дата", name: "date", widget: "datetime", format: "YYYY-MM-DD" }
      - { label: "Картинка", name: "image", widget: "image", required: false }
      - { label: "Описание картинки", name: "imageAlt", widget: "string", required: false }
      - { label: "Краткое описание", name: "excerpt", widget: "text", required: false }
      - label: "Видео (ссылка на YouTube)"
        name: "video"
        widget: "string"
        required: false
        hint: "Вставь ссылку на YouTube-видео, например https://www.youtube.com/watch?v=XXXXXXXXXXX"
      - { label: "Текст поста", name: "body", widget: "markdown" }
      - { label: "Черновик (не публиковать)", name: "draft", widget: "boolean", default: false }

  - name: "pages"
    label: "Страницы"
    label_singular: "Страница"
    folder: "src/content/pages"
    create: true
    delete: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}}"
    fields:
      - { label: "Заголовок", name: "title", widget: "string" }
      - { label: "Картинка", name: "image", widget: "image", required: false }
      - label: "Показывать в меню сайта"
        name: "showInMenu"
        widget: "boolean"
        default: true
      - label: "Порядок в меню (меньше число — левее)"
        name: "menuOrder"
        widget: "number"
        default: 0
        required: false
      - { label: "Текст", name: "body", widget: "markdown" }
